version: '3.8'

services:
  minio:
    image: minio/minio:latest
    container_name: afyaquik-minio
    ports:
      - "9000:9000"      # API port
      - "9001:9001"      # Console port
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # Optional: MinIO Client for bucket management
  minio-client:
    image: minio/mc:latest
    container_name: afyaquik-minio-client
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 minioadmin minioadmin123;
      /usr/bin/mc mb myminio/diagnostic-files --ignore-existing;
      /usr/bin/mc policy set public myminio/diagnostic-files;
      tail -f /dev/null
      "

volumes:
  minio_data:
    driver: local
